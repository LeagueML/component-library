import React, { Ref } from 'react';
import './LoadingCard.css'

function LoadingCard(props : {}, ref : Ref<HTMLDivElement>) {
  return (
    <div className='loading'></div>
  );
}

export default LoadingCard;
